1) IDE (или интегрированная среда разработки) — это программа, предназначенная для разработки программного обеспечения. 
Как следует из названия, IDE объединяет несколько инструментов, специально предназначенных для разработки. 
Эти инструменты обычно включают редактор, предназначенный для работы с кодом (например, подсветка синтаксиса и автодополнение);
инструменты сборки, выполнения и отладки; и определённую форму системы управления версиями.
 Я знаю Jupyter Notebooks, PyCharm, Spyder и  Visual Studio

2)Импортирует данные с помошью которых в среде разработки появляются дополнительные функции

3)Основные типы данных в питоне это int, float, list, str, tuple их можно узнать с помощью функции type()
4) Для нахождение деление без остатка можно использовать //, а для нахождение остатка %
5) a = "55" c = int(a)               b = 55 g = float(g)
6)import numpy as np     
  spisok = np.arange(1,11)
  zero = [0] * 10
7)len(zero)
out[]: 10
8) C помощью append(), C помощью insert(), C помощью remove()
9)Кортеж объявляется круглыми скобками tuple = (1, 2, 3). Список же квадратными list = [1, 2, 3]. 
10) 1. f = {}
    2. h=dict(g='s', l='d')
11)
    t = 'gl'
    h=dict(g='s', l='d')
    for i in h.keys():
        print(h[i], end='')

    for i in t: 
        print(h[i])
12)True это если какое-либо функция выполняется и оно является верным то это True а False это наоборот 
if- работает если выполняется какое-то выражение 
elif - работает если не выполняется if 
else - работает если if and elif False

13) for - она идет пока не достигнет назначенного предела 
    white - она выполняется пока какое-то выражение выполняется
14)
    break - это применяется когда нужно остановить какой-нибудь цикл
    continuе - применяется когда нужно продолжить какой-нибудь цикл
15) Суть понятия функции в том, что она, будучи связующим звеном между множествами значений, 
связывает с первым только один элемент второго множества.
16)Если функция не возвращает никакого значения, то вместо типа данных в объявлении и определении функции указывается ключевое слово void.
 Внутри такой функции оператора return может не быть, однако его можно использовать без указания значения 
для преждевременного выхода из функции.
17) Область видимости — часть программы, в пределах которой идентификатор, объявленный как имя некоторой программной сущности 
(обычно — переменной, типа данных или функции), остаётся связанным с этой сущностью, то есть позволяет посредством себя обратиться к ней.

18) это числовая функция {\displaystyle f(n)}f(n) числового аргумента, которая в своей записи содержит себя же. 
Такая запись позволяет вычислять значения {\displaystyle f(n)}f(n) на основе значений 
{\displaystyle f(n-1),f(n-2),\ldots }f(n-1),f(n-2),\ldots , подобно рассуждению по индукции. 
Чтобы вычисление завершалось для любого {\displaystyle n}n, 
необходимо, чтобы для некоторых {\displaystyle n}n функция была определена нерекурсивно (например, для {\displaystyle n=0,1}n=0,1).
F(n)
F(2) = F(n) - F(n-1)
Что-то такое называют рекурсивной функцией
19) NumPy это open-source модуль для python, который предоставляет 
общие математические и числовые операции в виде пре-скомпилированных, быстрых функций.
ndarray - это многомерный массив
20)Pandas – это быстрый, мощный, гибкий и простой в использовании инструмент для анализа и обработки данных 
с открытым исходным кодом, созданный на языке программирования Python.
Dataframe — это табличная структура данных























